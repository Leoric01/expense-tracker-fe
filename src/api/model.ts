/** Stub DTOs until Orval generates real API types. */
export interface BranchResponseDto {
  id: string;
  name?: string;
}

export interface MenuItemResponseDto {
  id: string;
  label?: string;
  path?: string;
}
