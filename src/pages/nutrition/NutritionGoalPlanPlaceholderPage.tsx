import { PageHeading } from '@components/PageHeading';
import { Box, Typography } from '@mui/material';
import { FC } from 'react';

/** Placeholder pro sekci 2 (Goal Plan) — nahradí se plnou implementací. */
export const NutritionGoalPlanPlaceholderPage: FC = () => (
  <Box>
    <PageHeading component="h1" gutterBottom>
      Cílový plán
    </PageHeading>
    <Typography color="text.secondary">Tato obrazovka bude doplněna v další sekci specifikace.</Typography>
  </Box>
);
